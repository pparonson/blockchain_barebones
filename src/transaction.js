export default class Transaction {
  constructor(fromAddress, toAddress, amount) {
    this.fromAddress = fromAddress,
    this.toAddress = toAddress,
    this.amount = amount
  }
}

// add mining reward

// create place to store pending transactions

// create method to mine new block of pending transactions
